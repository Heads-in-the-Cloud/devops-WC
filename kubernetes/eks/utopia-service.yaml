apiVersion: v1
kind: Service
metadata: 
  name: users-service
  namespace: nginx-ingress
  labels:
    app: users
spec:
  selector:
    app: users
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80



---

apiVersion: v1
kind: Service
metadata: 
  name: flights-service
  namespace: nginx-ingress
  labels:
    app: flights
spec:
  selector:
    app: flights
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80


---

apiVersion: v1
kind: Service
metadata: 
  name: bookings-service
  namespace: nginx-ingress
  labels:
    app: bookings
spec:
  selector:
    app: bookings
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

